<div class="row">
	<div class="col-xs-9">
		<div class="row">
			<h2><%= "#{@customer.family_name}#{@customer.first_name}" %>さんの会員情報編集</h2>
			<%= form_with model: @customer, url: admins_customer_path(@customer.id), method: :patch, local: true do |f| %>
			<div>
				<p>会員ID</p>
				<p><%= @customer.id %></p>
				<p>氏名</p>
				<%= f.text_field :family_name %>
				<%= f.text_field :first_name %>
				<p>フリガナ</p>
				<%= f.text_field :family_name_ruby %>
				<%= f.text_field :first_name_ruby %>
				<p>郵便番号</p>
				<%= f.text_field :postcode %>
				<p>住所</p>
				<%= f.text_field :address %>
				<p>電話番号</p>
				<%= f.text_field :tell_number %>
				<p>メールアドレス</p>
				<%= f.text_field :email %>
				<p>会員ステータス</p>
				<%= f.radio_button :is_valid, true %>有効会員 <%= f.radio_button :is_valid, false %>退会済み
			</div>
			<%= f.submit "変更を保存する" %>
			<% end %>
		</div>
	</div>
</div>